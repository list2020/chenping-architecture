package ${package}.dao;

import ${package}.entity.${className}Entity;
import org.apache.ibatis.annotations.Mapper;
import com.tiaim.framework.mybatis.BaseDao;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
@Mapper
public interface ${className}Dao extends BaseDao<${className}Entity> {
	
}
